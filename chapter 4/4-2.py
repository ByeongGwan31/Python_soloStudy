# 함수 안에서 선언된 변수의 효력 범위

a = 1
def vartest(a) :
    a = a + 1
#이곳에 쓰인 변수는 지역변수이다.
# 임시변수느낌이다. a라는 값이 밖에 있는 영향을 주지 못 한다.
# 함수 내에서 변수를 사용한다. 범위가 함수까지이다. 밖에는 영향을 미치지 못 한다.


# [!] 함수 안에 있는 변수는 함수 안에서만 사용된다.

vartest(a)
print(a)